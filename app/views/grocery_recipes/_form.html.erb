<%= form_for(@grocery_recipe) do |f| %>
  <% if @grocery_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grocery_recipe.errors.count, "error") %> prohibited this grocery_recipe from being saved:</h2>

      <ul>
      <% @grocery_recipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
